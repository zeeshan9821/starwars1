import React from 'react';
import Layout from './layout/layout.js';


const App = (props) => {
    return (
        <div>
            <Layout {...props} />
        </div>
    );
};

export default App;
